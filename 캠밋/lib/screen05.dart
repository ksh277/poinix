
import 'package:flutter/material.dart';

class Screen05 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen05')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen05 Item #$index'),
          subtitle: Text('This is an example item for Screen05'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen05 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen05')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen05 Item #$index'),
          subtitle: Text('This is an example item for Screen05'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen05 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen05')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen05 Item #$index'),
          subtitle: Text('This is an example item for Screen05'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen05 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen05')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen05 Item #$index'),
          subtitle: Text('This is an example item for Screen05'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen05 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen05')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen05 Item #$index'),
          subtitle: Text('This is an example item for Screen05'),
        ),
      ),
    );
  }
}
