
import 'package:flutter/material.dart';

class Screen11 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen11')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen11 Item #$index'),
          subtitle: Text('This is an example item for Screen11'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen11 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen11')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen11 Item #$index'),
          subtitle: Text('This is an example item for Screen11'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen11 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen11')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen11 Item #$index'),
          subtitle: Text('This is an example item for Screen11'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen11 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen11')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen11 Item #$index'),
          subtitle: Text('This is an example item for Screen11'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen11 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen11')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen11 Item #$index'),
          subtitle: Text('This is an example item for Screen11'),
        ),
      ),
    );
  }
}
