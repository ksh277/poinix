
import 'package:flutter/material.dart';

class Screen04 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen04')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen04 Item #$index'),
          subtitle: Text('This is an example item for Screen04'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen04 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen04')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen04 Item #$index'),
          subtitle: Text('This is an example item for Screen04'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen04 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen04')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen04 Item #$index'),
          subtitle: Text('This is an example item for Screen04'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen04 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen04')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen04 Item #$index'),
          subtitle: Text('This is an example item for Screen04'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen04 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen04')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen04 Item #$index'),
          subtitle: Text('This is an example item for Screen04'),
        ),
      ),
    );
  }
}
