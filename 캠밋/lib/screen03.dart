
import 'package:flutter/material.dart';

class Screen03 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen03')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen03 Item #$index'),
          subtitle: Text('This is an example item for Screen03'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen03 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen03')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen03 Item #$index'),
          subtitle: Text('This is an example item for Screen03'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen03 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen03')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen03 Item #$index'),
          subtitle: Text('This is an example item for Screen03'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen03 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen03')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen03 Item #$index'),
          subtitle: Text('This is an example item for Screen03'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen03 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen03')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen03 Item #$index'),
          subtitle: Text('This is an example item for Screen03'),
        ),
      ),
    );
  }
}
