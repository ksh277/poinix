
import 'package:flutter/material.dart';

class Screen13 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen13')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen13 Item #$index'),
          subtitle: Text('This is an example item for Screen13'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen13 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen13')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen13 Item #$index'),
          subtitle: Text('This is an example item for Screen13'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen13 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen13')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen13 Item #$index'),
          subtitle: Text('This is an example item for Screen13'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen13 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen13')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen13 Item #$index'),
          subtitle: Text('This is an example item for Screen13'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen13 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen13')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen13 Item #$index'),
          subtitle: Text('This is an example item for Screen13'),
        ),
      ),
    );
  }
}
