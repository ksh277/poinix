
import 'package:flutter/material.dart';

class Screen18 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen18')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen18 Item #$index'),
          subtitle: Text('This is an example item for Screen18'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen18 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen18')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen18 Item #$index'),
          subtitle: Text('This is an example item for Screen18'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen18 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen18')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen18 Item #$index'),
          subtitle: Text('This is an example item for Screen18'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen18 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen18')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen18 Item #$index'),
          subtitle: Text('This is an example item for Screen18'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen18 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen18')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen18 Item #$index'),
          subtitle: Text('This is an example item for Screen18'),
        ),
      ),
    );
  }
}
