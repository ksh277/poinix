
import 'package:flutter/material.dart';

class Screen01 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen01')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen01 Item #$index'),
          subtitle: Text('This is an example item for Screen01'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen01 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen01')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen01 Item #$index'),
          subtitle: Text('This is an example item for Screen01'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen01 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen01')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen01 Item #$index'),
          subtitle: Text('This is an example item for Screen01'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen01 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen01')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen01 Item #$index'),
          subtitle: Text('This is an example item for Screen01'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen01 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen01')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen01 Item #$index'),
          subtitle: Text('This is an example item for Screen01'),
        ),
      ),
    );
  }
}
