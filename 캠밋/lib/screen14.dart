
import 'package:flutter/material.dart';

class Screen14 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen14')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen14 Item #$index'),
          subtitle: Text('This is an example item for Screen14'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen14 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen14')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen14 Item #$index'),
          subtitle: Text('This is an example item for Screen14'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen14 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen14')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen14 Item #$index'),
          subtitle: Text('This is an example item for Screen14'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen14 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen14')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen14 Item #$index'),
          subtitle: Text('This is an example item for Screen14'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen14 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen14')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen14 Item #$index'),
          subtitle: Text('This is an example item for Screen14'),
        ),
      ),
    );
  }
}
