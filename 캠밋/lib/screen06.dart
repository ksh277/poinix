
import 'package:flutter/material.dart';

class Screen06 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen06')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen06 Item #$index'),
          subtitle: Text('This is an example item for Screen06'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen06 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen06')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen06 Item #$index'),
          subtitle: Text('This is an example item for Screen06'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen06 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen06')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen06 Item #$index'),
          subtitle: Text('This is an example item for Screen06'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen06 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen06')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen06 Item #$index'),
          subtitle: Text('This is an example item for Screen06'),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class Screen06 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen06')),
      body: ListView.builder(
        itemCount: 100,
        itemBuilder: (context, index) => ListTile(
          title: Text('Screen06 Item #$index'),
          subtitle: Text('This is an example item for Screen06'),
        ),
      ),
    );
  }
}
