name: cameet_project
description: Fully integrated Cameet Flutter app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  firebase_core: ^1.24.0
  firebase_auth: ^3.14.1
  firebase_messaging: ^14.2.3
  firebase_analytics: ^10.2.0
  cloud_firestore: ^4.9.1
  shared_preferences: ^2.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  kakao_map_flutter: ^1.0.1
  image_picker: ^0.8.7+4
  location: ^4.4.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
